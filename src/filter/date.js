// import Vue from "vue";
// Vue.filter("date",function (date) {
//   return new Date(date).toLocaleDateString();
// })

export default function (date) {
  return new Date(date).toLocaleDateString();
}